---
title: "Veo 3.1"
description: "使用 Google Veo 3.1 模型生成视频"
---

# Veo 3.1

Google Veo 3.1 是目前最先进的视频生成模型之一，支持高质量的文生视频和图生视频。

## 通过统一视频 API 调用

```
POST /v1/video/create
```

### 请求示例

<CodeGroup>

```bash cURL
curl -X POST https://crazyrouter.com/v1/video/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "veo3.1-components",
    "prompt": "A cinematic shot of a spaceship landing on Mars, dust clouds rising, golden hour lighting",
    "aspect_ratio": "16:9"
  }'
```

```python Python
import requests

response = requests.post(
    "https://crazyrouter.com/v1/video/create",
    headers={
        "Content-Type": "application/json",
        "Authorization": "Bearer YOUR_API_KEY"
    },
    json={
        "model": "veo3.1-components",
        "prompt": "A cinematic shot of a spaceship landing on Mars",
        "aspect_ratio": "16:9"
    }
)

task_id = response.json()["id"]
print(f"任务 ID: {task_id}")
```

</CodeGroup>

### 响应示例

```json
{
  "id": "veo_task_abc123",
  "status": "processing",
  "status_update_time": 1709123456
}
```

---

## 查询任务

```
GET /v1/video/query?id={task_id}
```

```bash cURL
curl "https://crazyrouter.com/v1/video/query?id=veo_task_abc123" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### 响应示例（已完成）

```json
{
  "id": "veo_task_abc123",
  "status": "completed",
  "status_update_time": 1709123520,
  "video_url": "https://crazyrouter.com/files/veo_abc123.mp4"
}
```

---

## 通过 Fal.ai 格式调用

```
POST /fal-ai/veo3/run
```

```bash cURL
curl -X POST https://crazyrouter.com/fal-ai/veo3/run \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "prompt": "A cinematic shot of a spaceship landing on Mars",
    "aspect_ratio": "16:9",
    "duration": "8s"
  }'
```

---

## 可用模型

| 模型 | 说明 |
|------|------|
| `veo3.1-components` | Veo 3.1，最新版本 |
| `veo3` | Veo 3 |
| `veo2` | Veo 2 |

<Note>
  Veo 3.1 生成的视频质量极高，但生成时间较长（通常 3-8 分钟）。建议轮询间隔为 15 秒。
</Note>
