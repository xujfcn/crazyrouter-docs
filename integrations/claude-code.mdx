---
title: 'Claude Code 配置教程'
description: '在 Claude Code 中通过 Anthropic Messages API 接入 Crazyrouter，并提供从 Git、Node.js、安装命令、环境变量到首次验证的完整步骤'
---

Claude Code 是目前最值得优先接入 Crazyrouter 的终端编码工具之一。它直接使用 Anthropic Messages API，适合代码阅读、修改、重构、命令执行、工具调用和长上下文仓库分析。

## 概览

通过环境变量，Claude Code 可以把 Anthropic 请求直接发到 Crazyrouter：

- 推荐协议：`Anthropic Messages API`
- Base URL：`https://crazyrouter.com`
- 中国地区优化 Base URL：`https://cn.crazyrouter.com`
- 认证变量：`ANTHROPIC_API_KEY`
- 推荐默认模型：`claude-sonnet-4-6`

<Tip>
Claude Code 自己会补完整的 Anthropic 请求路径，所以 Base URL 必须写根域名 `https://crazyrouter.com` 或 `https://cn.crazyrouter.com`，不要手动拼 `/v1` 或 `/v1/messages`。
</Tip>

<Card title="查看 Claude Code 一键配置仓库" icon="github" href="https://github.com/xujfcn/crazyrouter-claude-code">
  如果你想直接使用脚本安装 Claude Code 并写入 Crazyrouter 环境变量，可以查看 crazyrouter-claude-code 仓库。
</Card>

## 适合谁用

- 想把 Crazyrouter 作为 Claude Code 后端的开发者
- 想稳定使用工具调用、长上下文和终端编码工作流的人
- 想把 Claude Code 和 Cursor / Codex / Aider 分开计费的人
- 想在 Linux、macOS、Windows 上统一配置 CLI 的团队

## 使用协议

推荐协议：`Anthropic Messages API`

Claude Code 对接 Crazyrouter 时请使用：

```text
ANTHROPIC_BASE_URL=https://crazyrouter.com
```

中国地区需要走优化线路时请使用：

```text
ANTHROPIC_BASE_URL=https://cn.crazyrouter.com
```

不要写成：

- `https://crazyrouter.com/v1`
- `https://crazyrouter.com/v1/messages`
- `https://crazyrouter.com/v1/complete`
- `https://cn.crazyrouter.com/v1`
- `https://cn.crazyrouter.com/v1/messages`

## 系统要求与前置条件

| 项目 | 说明 |
|------|------|
| Crazyrouter 账号 | 先在 [crazyrouter.com](https://crazyrouter.com) 注册 |
| Crazyrouter token | 建议单独创建一个给 Claude Code 使用的 `sk-...` token |
| Git | 建议 `git 2.23+`，便于回滚和审查 AI 改动 |
| Node.js | 建议 `Node.js 18+` |
| Claude Code | 建议当前稳定版 |
| Claude 系列模型权限 | 至少放行 `claude-sonnet-4-6` |

建议初始白名单：

- `claude-sonnet-4-6`
- `claude-opus-4-6`

## 按操作系统的完整安装路径

### Windows 推荐路径

Claude Code 在 Windows 上最稳妥的路径是：`Git` + `Node.js` + `npm 全局安装 Claude Code` + `PowerShell` 写入环境变量。

推荐顺序：

1. 安装 Git
2. 安装 Node.js LTS
3. 使用 npm 安装 Claude Code
4. 用 PowerShell 写临时变量
5. 用 PowerShell 写用户级持久变量

推荐验证命令：

```powershell
git --version
node -v
npm -v
claude --version
where.exe git
where.exe node
where.exe claude
```

如果 `claude --version` 找不到命令，先关闭并重新打开 PowerShell，再重试。

### macOS 推荐路径

Claude Code 在 macOS 上最顺手的路径通常是：`Xcode Command Line Tools` + `Homebrew` + `Git` + `Node.js` + `npm 全局安装 Claude Code` + `~/.zshrc` 持久化环境变量。

推荐顺序：

1. 安装 Xcode Command Line Tools
2. 安装 Homebrew（如果你还没有）
3. 安装 Git、Node.js
4. 使用 npm 安装 Claude Code
5. 写入 `~/.zshrc`
6. 新开终端验证命令路径

推荐验证命令：

```bash
git --version
node -v
npm -v
claude --version
which git
which node
which claude
```

### 为什么这里不建议手动写 API 路径

Claude Code 走的是 Anthropic 原生协议。你只需要告诉它站点根地址：

```text
ANTHROPIC_BASE_URL=https://crazyrouter.com
```

中国地区优化线路同样只填写根地址：

```text
ANTHROPIC_BASE_URL=https://cn.crazyrouter.com
```

不要像 OpenAI 兼容客户端那样自己拼 `/v1`、`/v1/messages` 或具体 API 路径。

如果你想用脚本自动安装 Claude Code 并写入 Crazyrouter 相关环境变量，可以查看 [crazyrouter-claude-code](https://github.com/xujfcn/crazyrouter-claude-code)。该仓库提供 Windows、macOS 和 Linux 的一键配置脚本；本文档仍保留完整手动配置步骤，便于你审查每一项配置。

## 从零开始完整安装

<Steps>
  <Step title="第 1 步：安装 Git">
    如果你的机器还没有 Git，先装 Git，再继续后面的 Claude Code 配置。

    <Tabs>
      <Tab title="Windows PowerShell">
        ```powershell
        winget install --id Git.Git -e --source winget
        git --version
        ```
      </Tab>
      <Tab title="macOS">
        ```bash
        xcode-select --install
        git --version
        ```
        如果你已经使用 Homebrew，也可以：
        ```bash
        brew install git
        git --version
        ```
      </Tab>
      <Tab title="Ubuntu / Debian">
        ```bash
        sudo apt update
        sudo apt install -y git
        git --version
        ```
      </Tab>
    </Tabs>

    安装后建议补上全局身份信息：

    ```bash
    git config --global user.name "Your Name"
    git config --global user.email "you@example.com"
    git config --global init.defaultBranch main
    ```
  </Step>

  <Step title="第 2 步：安装 Node.js 18+">
    Claude Code 依赖 Node.js。先确认版本，再继续安装 Claude Code。

    <Tabs>
      <Tab title="Windows PowerShell">
        ```powershell
        winget install OpenJS.NodeJS.LTS
        node -v
        npm -v
        ```
      </Tab>
      <Tab title="macOS">
        ```bash
        brew install node
        node -v
        npm -v
        ```
      </Tab>
      <Tab title="Ubuntu / Debian">
        ```bash
        sudo apt update
        sudo apt install -y nodejs npm
        node -v
        npm -v
        ```
      </Tab>
    </Tabs>

    如果你装完后 `node -v` 仍低于 18，建议改用 `nvm` 或 Node 官方安装包升级后再继续。
  </Step>

  <Step title="第 3 步：安装 Claude Code">
    <Tabs>
      <Tab title="Windows PowerShell">
        ```powershell
        npm install -g @anthropic-ai/claude-code
        claude --version
        where.exe claude
        ```
      </Tab>
      <Tab title="macOS">
        ```bash
        npm install -g @anthropic-ai/claude-code
        claude --version
        which claude
        ```
      </Tab>
    </Tabs>

    <Warning>
    不要使用 `sudo npm install -g @anthropic-ai/claude-code`。如果全局安装遇到权限问题，先修复 npm/Node 环境，再继续安装。
    </Warning>
  </Step>

  <Step title="第 4 步：在 Crazyrouter 创建 Claude Code 专用 token">
    登录 Crazyrouter 后台，创建一个单独 token，名称建议直接写 `claude-code`。

    第一次只建议放行：

    - `claude-sonnet-4-6`
    - `claude-opus-4-6`

    同时建议给它设置独立额度，避免和 Cursor、Codex、OpenClaw 共用预算。
  </Step>

  <Step title="第 5 步：先在当前终端设置临时环境变量">
    先做临时配置，确认能跑通后，再写入持久配置。

    <Tabs>
      <Tab title="macOS / Linux">
        ```bash
        export ANTHROPIC_BASE_URL=https://crazyrouter.com
        export ANTHROPIC_API_KEY=sk-xxx
        echo $ANTHROPIC_BASE_URL
        echo $ANTHROPIC_API_KEY
        ```
      </Tab>
      <Tab title="Windows PowerShell">
        ```powershell
        $env:ANTHROPIC_BASE_URL = "https://crazyrouter.com"
        $env:ANTHROPIC_API_KEY = "sk-xxx"
        echo $env:ANTHROPIC_BASE_URL
        echo $env:ANTHROPIC_API_KEY
        ```
      </Tab>
    </Tabs>

    如果你在中国地区走优化线路，把 `ANTHROPIC_BASE_URL` 改成根域名 `https://cn.crazyrouter.com`，不要改成 `https://cn.crazyrouter.com/v1`。
  </Step>

  <Step title="第 6 步：把环境变量写入持久配置">
    临时变量只对当前终端有效。日常使用建议写入 shell 配置文件。

    <Tabs>
      <Tab title="Linux Bash">
        ```bash
        echo 'export ANTHROPIC_BASE_URL=https://crazyrouter.com' >> ~/.bashrc
        echo 'export ANTHROPIC_API_KEY=sk-xxx' >> ~/.bashrc
        source ~/.bashrc
        ```
      </Tab>
      <Tab title="macOS / Zsh">
        ```bash
        echo 'export ANTHROPIC_BASE_URL=https://crazyrouter.com' >> ~/.zshrc
        echo 'export ANTHROPIC_API_KEY=sk-xxx' >> ~/.zshrc
        source ~/.zshrc
        ```
      </Tab>
      <Tab title="Windows PowerShell">
        ```powershell
        [System.Environment]::SetEnvironmentVariable("ANTHROPIC_BASE_URL", "https://crazyrouter.com", "User")
        [System.Environment]::SetEnvironmentVariable("ANTHROPIC_API_KEY", "sk-xxx", "User")

        $env:ANTHROPIC_BASE_URL = "https://crazyrouter.com"
        $env:ANTHROPIC_API_KEY = "sk-xxx"
        ```
      </Tab>
    </Tabs>

    如果使用中国地区优化线路，持久变量中的 `ANTHROPIC_BASE_URL` 也应写成 `https://cn.crazyrouter.com`。

    完成后建议新开一个终端，再执行：

    <Tabs>
      <Tab title="macOS / Linux">
        ```bash
        claude --version
        echo $ANTHROPIC_BASE_URL
        ```
      </Tab>
      <Tab title="Windows PowerShell">
        ```powershell
        claude --version
        echo $env:ANTHROPIC_BASE_URL
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step title="第 7 步：准备你的 Git 仓库">
    Claude Code 会读写文件、执行命令。第一次接入时，建议先在你熟悉的小仓库中验证。

    如果当前目录还不是 Git 仓库：

    ```bash
    git init
    git add .
    git commit -m "chore: initial snapshot before Claude Code"
    ```

    如果已经是现有仓库，至少先确认工作区是你自己能接受的状态：

    ```bash
    git status
    ```
  </Step>

  <Step title="第 8 步：启动 Claude Code 并完成第一次验证">
    进入项目目录后运行：

    ```bash
    cd /path/to/your/project
    claude
    ```

    第一次验证建议按下面顺序进行：

    1. 先发一句：`请只回复 OK`
    2. 再发一句：`只读取当前仓库结构，不要修改任何文件`
    3. 最后再做低风险任务，例如：`请找出 README 里的明显错别字，先不要直接改文件`

    当这三步都正常返回，且 Crazyrouter 后台出现请求日志，就说明链路已经跑通。
  </Step>
</Steps>

## 推荐模型配置

| 使用场景 | 推荐模型 | 原因 |
|----------|----------|------|
| 默认主力 | `claude-sonnet-4-6` | 质量、速度、成本平衡最好，适合大多数编码任务 |
| 高难度重构 | `claude-opus-4-6` | 更强的复杂推理、规划和代码理解 |
| 长上下文仓库分析 | `claude-sonnet-4-6` | 稳定，适合长期会话 |
| 成本敏感验证 | `claude-sonnet-4-6` | 先把主链路跑通最重要 |

建议先把默认工作流稳定在 `claude-sonnet-4-6`，只有在确实遇到复杂任务时再切 `claude-opus-4-6`。

## 在 Claude Code 中使用非 Claude 模型

除了原生 Claude 系列以外，Crazyrouter 还把若干第三方模型转译成了 Anthropic Messages 协议，可以直接在 Claude Code 中通过切换模型名使用。下面列出**经过实测**（文本 / system / 工具调用 / 流式 SSE 四项能力均通过）可在 Claude Code 中工作的型号。

### 实测可用模型清单

| 模型 ID | 提供方 | 工具调用 | 流式 | thinking | 使用场景 |
|---------|--------|----------|------|----------|----------|
| `deepseek-v4-pro` | DeepSeek | ✅ | ✅ | 无 | 通用编码、重构，性价比高 |
| `deepseek-v4-flash` | DeepSeek | ✅ | ✅ | ✅ 原生 | 带思考链，适合复杂分析 |
| `deepseek-reasoner` | DeepSeek | ✅ | ✅ | 推理 | 数学 / 复杂逻辑 |
| `deepseek-chat` / `deepseek-v3` / `deepseek-v3.2` / `deepseek-r1` | DeepSeek | ✅ | ✅ | — | 通用文本任务 |
| `MiniMax-M2.7` | MiniMax | ✅ | ✅ | ⚠️ 走 text 块，含 `<think>` 标签 | 长上下文中文任务 |
| `MiniMax-M2.5` | MiniMax | ✅ | ✅ | ⚠️ 走 text 块 | 通用对话 |
| `MiniMax-M2.1` | MiniMax | ✅ | ✅ | 无 | 不带思考链，输出更干净 |
| `kimi-k2.5` / `kimi-k2` / `kimi-k2-instruct` | Moonshot Kimi | ✅ | ✅ | — | 长上下文、中文工程任务 |
| `kimi-k2-thinking` | Moonshot Kimi | ✅ | ✅ | 思考链 | 复杂推理 |
| `kimi-k2-0711-preview` / `kimi-k2-0905-preview` | Moonshot Kimi | ✅ | ✅ | — | 快照版本 |

> 上述测试基于 `https://crazyrouter.com/v1/messages` + `anthropic-version: 2023-06-01`，请求体使用标准 Anthropic Messages 报文。

<Warning>
以下模型当前在 Anthropic 协议下返回 `get_channel_failed`（属上游渠道未上架，非协议不兼容），请暂时通过 `/v1/chat/completions` 走 OpenAI 协议接入：

- `moonshot-v1-8k` / `moonshot-v1-32k` / `moonshot-v1-128k`
- `kimi-k2-0905`（不带 `-preview` 后缀）

`grok-*`、`coze`、`jimeng`、`baidu`、`zhipu`、`tencent`、`xunfei`、`mistral`、`cohere`、`palm` 等渠道**未实现** Anthropic 协议转换，无法在 Claude Code 中使用。
</Warning>

### 切换模型的方式

Claude Code 通过 `ANTHROPIC_MODEL` 环境变量或会话内 `/model` 命令选择模型。把模型名替换成上表中任意一个即可。

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    export ANTHROPIC_BASE_URL=https://crazyrouter.com
    export ANTHROPIC_API_KEY=sk-xxx

    # 默认切到 DeepSeek V4 Pro
    export ANTHROPIC_MODEL=deepseek-v4-pro
    claude

    # 或临时切到 Kimi 思考版
    ANTHROPIC_MODEL=kimi-k2-thinking claude

    # 或临时切到 MiniMax
    ANTHROPIC_MODEL=MiniMax-M2.1 claude
    ```
  </Tab>
  <Tab title="Windows PowerShell">
    ```powershell
    $env:ANTHROPIC_BASE_URL = "https://crazyrouter.com"
    $env:ANTHROPIC_API_KEY  = "sk-xxx"

    $env:ANTHROPIC_MODEL = "deepseek-v4-pro"
    claude

    # 或临时切换
    $env:ANTHROPIC_MODEL = "kimi-k2-thinking"; claude
    $env:ANTHROPIC_MODEL = "MiniMax-M2.1"; claude
    ```
  </Tab>
</Tabs>

也可以在 Claude Code 启动后通过会话命令切换：

```text
/model deepseek-v4-pro
/model kimi-k2-thinking
/model MiniMax-M2.1
```

### 使用建议与已知差异

- **DeepSeek 系列**：协议兼容度最高，工具调用、流式输出、`stop_reason=tool_use` 与 Anthropic 标准一致；`deepseek-v4-flash` 会原生返回 `thinking` 块，Claude Code 能正确展示思考过程。**首选 `deepseek-v4-pro`**。
- **Kimi 系列**：长上下文表现稳定，工具调用正常。`kimi-k2-thinking` 会在响应中带思考链，适合复杂任务。
- **MiniMax 系列**：是定价表里 `supported_endpoint_types` 显式标注 `anthropic` 的少数模型，协议层最贴合。但 `M2.5` / `M2.7` 会把思考过程作为普通 text 块返回，**前后包含 `<think>...</think>` 标签**——Claude Code 会原样显示。两种规避方式：
  1. 使用 `MiniMax-M2.1`（无 thinking）；
  2. 在 system 提示中明确："不要输出 `<think>` 标签或思考过程，只返回最终答案"。
- **Token 白名单**：如果你的 Claude Code 专用 token 已开启模型白名单，需要在 Crazyrouter 后台把上面想用的模型也加入白名单，否则会返回 `403 model not allowed`。
- **思考链与计费**：带思考的模型（`-thinking`、`-flash`、`reasoner`、`MiniMax-M2.5/2.7`）会消耗额外 output token，建议给 Claude Code 单独配预算上限。

### 一条命令快速验证某个模型是否可用

```bash
curl -sS https://crazyrouter.com/v1/messages \
  -H "Authorization: Bearer $ANTHROPIC_API_KEY" \
  -H "anthropic-version: 2023-06-01" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "deepseek-v4-pro",
    "max_tokens": 32,
    "messages": [{"role":"user","content":"reply OK"}]
  }'
```

返回 200 且 `content[].text` 不为空即代表 Claude Code 可以正常使用该模型。

## Token 设置最佳实践

| 设置 | 建议 | 说明 |
|------|------|------|
| 专用 token | 必须 | Claude Code 不要和 Cursor、Codex、OpenClaw 共用 token |
| 模型白名单 | 强烈建议 | Claude Code 场景通常只需要 1 到 2 个 Claude 模型 |
| IP 限制 | 固定出口环境建议开启 | 笔记本经常变 IP 时谨慎使用 |
| 配额上限 | 强烈建议 | Claude Code 长会话 + 工具调用容易持续消耗额度 |
| 开发 / 团队分离 | 建议 | 每个开发者、每台共享主机都单独使用 token |
| 泄露轮换 | 必须 | shell 历史、录屏或共享终端暴露 token 后立即轮换 |

## 验证清单

- [ ] `git --version` 正常
- [ ] `node -v` 不低于 18
- [ ] `claude --version` 正常
- [ ] `ANTHROPIC_BASE_URL` 已设置为 `https://crazyrouter.com` 或 `https://cn.crazyrouter.com`
- [ ] `ANTHROPIC_API_KEY` 已正确设置
- [ ] Claude Code 可以正常启动
- [ ] 第一条普通文本请求成功返回
- [ ] 只读仓库分析可以正常工作
- [ ] Crazyrouter 后台日志中能看到 Claude Code 请求
- [ ] token 配额与模型白名单符合预期

## 常见错误与修复

| 现象 | 常见原因 | 修复方式 |
|------|----------|----------|
| `claude: command not found` | Claude Code 没装成功，或 npm 全局路径没进 PATH | 重新安装，确认 `npm bin -g` 所在目录已进 PATH |
| `node` 版本过低 | 本机 Node 版本低于要求 | 升级到 Node.js 18+ 后重装 Claude Code |
| 401 unauthorized | `ANTHROPIC_API_KEY` 无效、过期或复制错误 | 重新生成 token 并重新设置环境变量 |
| 403 / model not allowed | token 没有放行当前 Claude 模型 | 在 Crazyrouter token 设置中放行所需模型 |
| 404 | Base URL 写成 `/v1`、`/v1/messages` 等错误路径 | 改回 `https://crazyrouter.com` 或 `https://cn.crazyrouter.com` |
| 日志路径出现 `/v1/v1/messages`、`/v1/v1/models` 或 `/v1/messages/v1/messages` | Claude Code 的 Base URL 中误带了 `/v1` 或完整端点，客户端又自动追加了一次路径 | 把 `ANTHROPIC_BASE_URL` 改成根域名，不要带任何路径 |
| Claude Code 启动后仍走旧配置 | 新环境变量没有重新加载 | 新开终端，或重新 `source ~/.bashrc` / `source ~/.zshrc` |
| Git 改动太乱 | 没先做仓库快照就开始让 AI 改代码 | 先提交初始快照，再逐轮修改 |
| 成本超预期 | 长上下文 + 多轮工具调用 + 长时间会话 | 缩短会话、拆分任务、给 Claude Code 单独设预算 |

## 性能与成本建议

- 默认先用 `claude-sonnet-4-6`
- 只有在复杂架构分析、长链路重构时再切 `claude-opus-4-6`
- 第一次接入先用小仓库验证，不要直接对大型生产仓库全量操作
- 每次进入新任务前先看一下 `git status`
- 工具调用频繁时，更要回看 Crazyrouter 日志和 token 消耗

## FAQ

### Claude Code 里 Base URL 应该写什么？

写根域名：`https://crazyrouter.com`

中国地区优化线路写根域名：`https://cn.crazyrouter.com`

### 为什么这里不能写 `/v1`？

因为 Claude Code 自己会补 Anthropic Messages API 路径。你只需要提供站点根地址。

### 我在 Windows 上应该用什么方式？

如果你主要是命令行开发，优先用 PowerShell 或 WSL2。无论哪种方式，都先保证 Git、Node.js、Claude Code 自己都已正确安装。

### Windows 上推荐用 PowerShell 还是 Git Bash？

首次配置时优先用 PowerShell。环境变量写入、`where.exe` 验证和用户级变量持久化都更直接。

### 第一次为什么要先做 Git 快照？

因为 Claude Code 会改文件、跑命令。先做快照，出问题时你更容易 review 和回退。

### 第一个推荐模型是什么？

先用 `claude-sonnet-4-6`。它通常是最稳的基线模型。

<Note>
如果你优先追求 Claude 系列模型 + 终端编码体验，Claude Code 应该放在 Crazyrouter 编码工具接入顺序的最前面。
</Note>

<Card title="查看 crazyrouter-claude-code 仓库" icon="github" href="https://github.com/xujfcn/crazyrouter-claude-code">
  查看一键配置脚本、README 和最新使用说明。
</Card>
